import numpy as np

def srm3d(I, Q, dim):
    if len(locals()) <= 2:
        dim = 2
    if len(locals()) == 1:
        Q = 256
    smallest_region_allowed = 1
    size_image = [np.shape(I)[0], np.shape(I)[1]]
    if(dim==2):
        [Ix, Iy] = 
    print('A')
