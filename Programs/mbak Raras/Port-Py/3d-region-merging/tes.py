import numpy as np
from matplotlib.pyplot import hist

# a = np.zeros((256, 256))
# print(a.shape)
# a = a.flatten()

# a = np.random.randint(low=1, high=100000, size=4000)
# print(a[20], a[22])
# ab = a + 1
# print(ab[20], ab[22])
# print(ab.size)

# print(np.amin(a))

import csv

def histogram(datas):
    datas = datas.flatten(1)
    return h


# results = []
# with open("tes.csv") as csvfile:
#     reader = csv.reader(csvfile)  # change contents to floats
#     for row in reader:  # each row is a list
#         results.append(row[0])
# # print(results)
# ima = np.array(results).astype(np.int)
# # print(np.amin(ima))
# h, w = histogram(ima)
# # print(h)
# # print(results[0])
#

if __name__ == '__main__':
    # a = np.random.randint(0, 10, (266, 266))
    # a = np.zeros((266,266,1))
    # a[23][32] = np.nan
    # print(a[23][32])
    # np.nan_to_num(a, copy=False).astype(int)
    # print(a[23][32])
    a = np.zeros((1, 236))
    print(a.size)


